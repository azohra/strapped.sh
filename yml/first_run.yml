strapped:
  repo: https://repo.strapped.sh

bash:
  echo:
    - { msg: "Woop woop! strapped.sh is ready to rock." }
    - { msg: "use flag '--yml /path/to/strapped.yml' to #StayStrapped" }
    - { msg: "Made with '<3' by Azohra" }
