strap:
  namespace: unix_utils

  version: latest

  description: "performs unix commands"

  compatability: OSX

  deps:
    - echo

  vars:
    - HOME

  routines:
    mkdir:
      exec: "mkdir ${dir}"
      message: "making ${dir}"
      emoji: 📂
      input:
        - { dir: ~/photos }
        - { dir: /www/_static }
    touch:
      exec: "touch ${file}"
      message: "making ${file}"
      emoji: 👉
      input:
        - { file: .bashrc }
    echo:
      exec: "echo \"🗣 ${msg}\""
      emoji: 🗣
      input:
        - { msg: "Hello world" }