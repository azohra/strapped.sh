strap:
  namespace: mac_utils

  description: "performs mac terminal commands"

  compatability: OSX

  deps:
    - mkdir
    - touch

  routines:
    mkdir:
      exec: "mkdir ${dir}"
      message: "making ${dir}"
      emoji: 📂
      input:
        - { dir: ~/photos }
        - { dir: /www/_static }
    touch:
      exec: "touch ${file}"
      message: "making ${file}"
      emoji: 👉
      input:
        - { file: .bashrc }